import { Inter, Khand, Lusitana } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const khand = Khand({ subsets: ['latin'], weight: ['500'] });
export const lusitana = Lusitana({ subsets: ['latin'], weight: ['700'] });
